<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstappwithjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstAppWithJava">
        <activity
            android:name=".AddActivity"
            android:label="Add Member"
            android:parentActivityName=".ViewMembers"></activity>
        <activity
            android:name=".ViewMembers"
            android:label="My Team"
            android:parentActivityName=".HomePage"></activity>
        <activity
            android:name=".HomePage"
            android:label="Home" />
        <activity
            android:name=".MainActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>